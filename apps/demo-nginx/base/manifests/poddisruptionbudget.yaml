apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: demo-nginx
  labels:
    app.kubernetes.io/name: demo-nginx
    owner: platform
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: demo-nginx
